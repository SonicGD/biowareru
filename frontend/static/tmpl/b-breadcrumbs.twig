<nav class="breadcrumbs__block">
    <ul class="breadcrumbs">
        {% for crumb in this.context.breadCrumbs %}
            <li class="breadcrumbs__item">
                <a class="breadcrumbs__link" href="{{ crumb.url }}">{{ crumb.title|raw }}</a>
            </li>
        {% endfor %}
    </ul>
</nav>